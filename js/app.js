jQuery(".fittext").fitText(1.9, {  maxFontSize: '50px' });

$(document).ready(function(){
    $('.slider').slick({
        prevArrow: $('#button-slider-previous'),
        nextArrow: $('#button-slider-next')
    });
});